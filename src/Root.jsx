/** @format */

import React from 'react';
import Sidebar from './Components';

const Root = () => {
	return (
        <div style={{ width: '100%', height: '100vh', background: 'blue', paddingTop: '0.8%' }}>
			<Sidebar />;
		</div>
	);
};

export default Root;
